import { IThemeDimension } from '../types';

const dimens: IThemeDimension = {
  // headerButtonSize: 23,
  // headerHeight: 50,
  borderRadius: 4,
  borderRadius2: 2,
  borderRadius4: 4,
  borderRadius8: 8,
  borderRadiusFull: 9999,
  // badgeSize: 18,

  // defaultButtonWidth: screenWidth - 60,
  // defaultButtonHeight: 42,
  // defaultButtonRadius: 5,

  // defaultLineWidth: 0.3,

  // defaultInputBoxHeight: 40,
};

export default dimens;
